/**
 * [INPUT]: 依赖渠道、模板、草稿、参考图选择与生成回调
 * [OUTPUT]: 对外提供封面创作输入表单与平台生成方案提示
 * [POS]: Cover Studio 的主编辑区；只收集意图并交由根组件持久化或发送 Agent
 * [PROTOCOL]: 变更时更新此头部，然后检查 README.md
 */
import { Sparkles } from "lucide-react";
import type { Channel, Draft, Template } from "./types";
import { ReferenceStrip } from "./asset-picker";
import { Button, Card } from "./ui";

export function CoverComposer({ channels, draft, templates, busy, onChange, onGenerate, onOpenReferences, onRemoveReference }: { channels: Channel[]; draft: Draft; templates: Template[]; busy: boolean; onChange: (next: Draft) => void; onGenerate: () => void; onOpenReferences: () => void; onRemoveReference: (id: string) => void }) {
  const selected = templates.find((item) => item.id === draft.templateId);
  const channel = channels.find((item) => item.id === draft.channel) ?? channels[0];
  const chooseChannel = (next: Channel) => onChange({ ...draft, channel: next.id, width: next.width, height: next.height });
  return <Card className="h-fit overflow-hidden"><header className="flex items-center justify-between border-b px-5 py-4"><div><p className="font-mono text-[10px] font-semibold tracking-[0.16em] text-primary">INPUT</p><h2 className="mt-1 text-sm font-semibold">生成参数</h2></div><span className="rounded-md bg-muted px-2 py-1 font-mono text-[10px] text-muted-foreground">FORM</span></header><section className="p-5"><Section title="发布渠道与尺寸" detail="选择最终投放画布。" /><div className="mt-3 grid gap-2 sm:grid-cols-2">{channels.map((item) => <button aria-pressed={item.id === channel.id} className={`rounded-lg border p-3 text-left transition ${item.id === channel.id ? "border-primary bg-primary/5 ring-1 ring-primary/30" : "hover:border-primary/45 hover:bg-muted/45"}`} key={item.id} onClick={() => chooseChannel(item)} type="button"><span className="block text-xs font-semibold">{item.label}</span><span className="mt-1 block font-mono text-[10px] text-muted-foreground">{item.width} × {item.height}</span></button>)}</div></section><section className="border-t p-5"><Section title="视觉模板" detail="模板决定基础提示词与构图语言。" /><div className="mt-3 grid gap-3 sm:grid-cols-3 xl:grid-cols-1">{templates.map((template, index) => <button aria-pressed={template.id === selected?.id} className={`overflow-hidden rounded-lg border text-left transition ${template.id === selected?.id ? "border-primary ring-1 ring-primary/30" : "hover:border-primary/45"}`} key={template.id} onClick={() => onChange({ ...draft, templateId: template.id })} type="button"><span className={`block h-12 bg-gradient-to-br ${["from-emerald-950 via-emerald-700 to-amber-100", "from-amber-400 via-rose-500 to-violet-900", "from-teal-100 via-slate-500 to-rose-300"][index % 3]}`} /><span className="block p-3"><strong className="block text-xs">{template.name}</strong><span className="mt-1 block truncate text-[10px] text-muted-foreground">{template.prompt}</span></span></button>)}</div></section><section className="border-t p-5"><Section title="参考封面" detail="仅作视觉参考，始终保留原素材引用。" /><div className="mt-3"><ReferenceStrip ids={draft.referenceAssetIds} onOpen={onOpenReferences} onRemove={onRemoveReference} /></div></section><section className="border-t p-5"><Section title="补充要求" detail="和模板一起组成最终创作意图。" /><label className="mt-3 block text-xs font-medium" htmlFor="cover-brief">画面要求</label><textarea className="mt-2 block min-h-36 w-full rounded-lg border bg-background px-3 py-2.5 text-sm leading-6 outline-none transition focus:border-primary focus:ring-2 focus:ring-primary/20" id="cover-brief" onChange={(event) => onChange({ ...draft, brief: event.target.value })} placeholder="例如：主体放在右侧，左侧留出标题区域；整体克制、有科技杂志感。" value={draft.brief} /><div className="mt-4"><p className="text-xs leading-5 text-muted-foreground">{selected ? `将使用「${selected.name}」模板，${draft.referenceAssetIds.length ? `并参考 ${draft.referenceAssetIds.length} 张图片。` : "不附加参考图。"}` : "先选择一个视觉模板。"}</p><Button className="mt-4 w-full" disabled={!selected || busy} onClick={onGenerate} type="button"><Sparkles className="size-3.5" />{busy ? "正在交给 Agent…" : "交给 Agent 生成"}</Button></div></section></Card>;
}

function Section({ title, detail }: { title: string; detail: string }) { return <div className="flex items-baseline justify-between gap-4"><h2 className="text-sm font-semibold">{title}</h2><p className="text-[11px] text-muted-foreground">{detail}</p></div>; }
